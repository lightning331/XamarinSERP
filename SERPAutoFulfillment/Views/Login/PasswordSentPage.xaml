<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SERPAutoFulfillment.Views"
             x:Class="SERPAutoFulfillment.Views.PasswordSentPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical"
                     VerticalOptions="FillAndExpand"
                     HorizontalOptions="FillAndExpand">

            <local:TopNavigateView BindingContext="{Binding}" />

            <Label Margin="15, 30, 15, 0" Text="PASSWORD SENT" TextColor="Black" FontSize="18"/>
            <Label Margin="60, 30, 60, 0" FontSize="Medium" Text="Your password has been sent to the provided email"/>

            <StackLayout Margin="0, 0, 0, 30" VerticalOptions="EndAndExpand" HorizontalOptions="CenterAndExpand">
                <Button
                    Text="OK"
                    TextColor="White"
                    BackgroundColor="{StaticResource PrimaryRed}"
                    Command="{Binding GoToLoginCommand}"
                    CornerRadius="10"
                    />
            </StackLayout>

        </StackLayout>
    </ContentPage.Content></ContentPage>
